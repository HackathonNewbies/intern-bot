# Task A: Slack integration implementation plan

Goal: private onboarding, selected-thread access, native briefing cards, action routing, and app wiring on the backup branch.

Architecture: managed Channels remains the ingress/UI surface. Authenticate the workspace, actor, and one-to-one DM before exposing personal state or agent tools. Persist role, up to three goals, selected thread references, and schedule consent in an isolated local store. Use an optional owner-bound Slack user token for channel thread reads and a same-workspace bot token for proactive private delivery. Task B owns extraction/memory and Task C can replace the briefing/worker ports.

- [x] Implement and test identity, selection parsing, profile persistence, and cross-owner denial.
- [x] Implement and test Slack API identity checks, pagination, exact source links, failed reads, and private delivery.
- [x] Implement native onboarding/selection/briefing cards and authenticated action routing; wire existing memory tools and fresh-run adapter only into verified DMs.
- [x] Implement optional background transport and consent/configuration surfaces; keep scheduling/content replaceable by Task C.
- [x] Exercise fixture workflow and failure paths, run typecheck and verify, review diff, document live setup and remaining account-dependent verification.

No credentials or current uncommitted Task B changes will be copied into the backup worktree. Local fixture tests do not establish live Slack delivery. Calendar remains Task C's optional provider-dependent feature.
